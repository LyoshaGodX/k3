# Completion Checklist
- Run `node --check script.js` after JavaScript or interaction-related edits.
- Visually inspect affected sections at mobile and desktop widths; useful widths are 320px, 390px, 768px, and desktop.
- Watch for grid min-width overflow with long Russian words in Unbounded, lens positioning after layout changes, and `reveal` transforms overriding hover transforms.
- Confirm contact links/form target `hello@k-3.top` when contact behavior changes.
- There is currently no repo-defined lint, formatter, or automated test suite.